services:
  php-apache:
    build: .
    container_name: sneaker-shop-backend
    ports:
      - "8080:80"
    volumes:
      - ./backend:/var/www/html
    environment:
      - PHP_ERROR_REPORTING=E_ALL
      - PHP_DISPLAY_ERRORS=On
    restart: unless-stopped

